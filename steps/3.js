/*
 All apps start with a window.

 Windows are created using the method "createWindow" in "Ti.UI".

 We'll create a window and store a reference to it in the variable "win".
 */

var win = Ti.UI.createWindow();